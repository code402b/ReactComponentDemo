import styled from 'styled-components';

import FlexContainer from './FlexContainer.styled.js';

const Header = styled(FlexContainer)`
  align-items: center;
  justify-content: center;
`

export default Header;